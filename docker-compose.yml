services:
  web:
    build: .
    container_name: secure-devsecops-app
    ports:
      - "5000:5000"
    environment:
      APP_ENV: production
      APP_VERSION: "1.0.0"
    restart: unless-stopped
